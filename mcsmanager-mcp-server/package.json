{
  "name": "mcsmanager-mcp-server",
  "version": "0.1.0",
  "description": "MCP stdio server for AstrBot to query and operate MCSManager instances safely.",
  "type": "module",
  "bin": {
    "mcsmanager-mcp-server": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "npm run build && node --test \"dist/**/*.test.js\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "license": "Apache-2.0"
}
